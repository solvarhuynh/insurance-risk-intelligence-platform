# P1-DQ-01 — Source và staging data quality

Ngày: 2026-09-22

Kết quả: PASS

## Implementation và evidence

`migrations/V8__create_data_quality_gate.sql` tạo `dq.QualityCheckLog` và `dq.sp_RunQualityGate`; `sql/07_run_quality_gate.sql` chạy production gate. Hard rules PASS: required columns, partition reconciliation, technical identity, numeric required/non-negative, State pair/mapping và claim count/amount pair.

Warning audit `STG_LOGICAL_DUPLICATE_AUDIT` PASS với đúng 14 logical duplicate được preserve; đây không phải hard violation. Không có raw file nào bị sửa.

## Tiêu chí PASS

- [x] Có rule executable cho schema, source-to-staging, null/range, categorical State consistency, duplicate audit và derived denominator policy.
- [x] Tất cả hard contractual rule production PASS.

## Bước tiếp theo

`P1-DQ-02` — xác minh warehouse integrity qua cùng gate.
