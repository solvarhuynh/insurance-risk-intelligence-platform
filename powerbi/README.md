# Power BI artifact handoff

`docs/bi/semantic-model.md` là semantic contract và build checklist cho hai subject area độc lập.

Status: `BLOCKED_MANUAL` — môi trường hiện tại không có Power BI Desktop, `pbi-tools` hay automation supported để tạo/refresh artifact thật. Không đặt `.pbix` placeholder vào repository, vì file đó sẽ giả completion.
