# P1-WF-04 — Chuẩn hóa brvehins1 trên toàn repository

Ngày: 2026-09-22

Kết quả: PASS

## Mục tiêu

Thiết lập `brvehins1` là nguồn duy nhất của pipeline hiện hành, loại bỏ hướng dẫn vận hành theo kiến trúc đã thay thế khỏi tài liệu hiện hành, và nâng static validator để kiểm tra đúng năm partition chuẩn mà không đọc toàn bộ dữ liệu.

## File đã thay đổi

- `README.md`
- `docs/architecture/architecture-explained.md`
- `docs/architecture/data-dictionary.md`
- `docs/architecture/repository-structure.md`
- `docs/guides/glossary.md`
- `docs/guides/how-to-run.md`
- `docs/reports/insights.md`
- `docs/specs/implementation-guide.md`
- `scripts/validate_repo.py`
- `reports/checkpoints/P1-WF-04.md`
- `log/progress-log.md`

Không có file raw nào bị sửa.

## Lệnh đã chạy

```text
git status --short
git diff --stat
git diff
rg --files
Get-Content trên rules, docs, log, scripts, migrations, sql, ml, dags, compose và notebook
python -c <CSV streaming count/header inspection>
python scripts/validate_repo.py
git diff --check
rg -n -i <legacy-reference patterns>
```

## Bằng chứng dữ liệu chuẩn

| Partition | Kích thước (bytes) | Số dòng | Số cột |
|---|---:|---:|---:|
| `brvehins1a.csv` | 60,499,054 | 393,071 | 23 |
| `brvehins1b.csv` | 60,497,433 | 393,071 | 23 |
| `brvehins1c.csv` | 60,481,086 | 393,071 | 23 |
| `brvehins1d.csv` | 60,491,192 | 393,071 | 23 |
| `brvehins1e.csv` | 60,501,172 | 393,071 | 23 |
| Tổng | 302,469,937 | 1,965,355 | 23 |

Năm header hoàn toàn bằng nhau. `scripts/validate_repo.py` chỉ đọc header bằng `csv.reader`, xác minh đúng năm file, file không rỗng và header bằng `CANONICAL_HEADER`; script không nạp toàn bộ khoảng hai triệu dòng.

File `data/raw/susep.gov.br/insurance_dataset.csv` vẫn tồn tại và được ghi rõ là **LEGACY / NON-CANONICAL**.

## Quyết định

1. `brvehins1` là nguồn chuẩn duy nhất cho mọi bước foundation.
2. Raw là bất biến; không ghi staging, prediction hoặc output vào `data/raw/`.
3. Tài liệu hiện hành không còn hướng dẫn tải hoặc sử dụng nguồn kiến trúc cũ.
4. Các mã cũ trong `migrations/`, `sql/`, `ml/` và `dags/` được phân loại **STALE SCAFFOLD — REQUIRES REFACTOR**; không bị xóa trong stage này.
5. Không giữ bất kỳ tiêu chí nghiệm thu ML theo ngưỡng chưa có bằng chứng thực nghiệm.

## Phân loại kết quả tìm kiếm tham chiếu cũ

| Vị trí | Phân loại | Xử lý |
|---|---|---|
| `log/progress-log.md` | HISTORICAL LOG | Giữ nguyên theo quy tắc append-only. |
| `migrations/` và `sql/` | STALE CODE SCAFFOLD | Refactor ở các stage infrastructure, ingest, DWH và DQ. |
| `ml/` và `dags/` | STALE CODE SCAFFOLD | Hoãn refactor cho milestone sau foundation. |
| `scripts/validate_repo.py` | VALID GENERAL REFERENCE | Chứa danh sách thuật ngữ bị cấm để ngăn tài liệu hiện hành quay lại kiến trúc cũ. |
| README và `docs/` hiện hành | CURRENT DOC | Không còn chỉ dẫn kiến trúc cũ; xác minh bằng validator. |

## Giả định và giới hạn

- Số dòng được tính bằng streaming CSV; chưa thực hiện EDA, dtype profiling, null/duplicate profiling hoặc suy diễn grain.
- Không suy diễn business identifier từ header.
- Docker, SQL Server, migration, staging, DWH, DQ, ML và Airflow chưa có bằng chứng runtime ở stage này.

## Tiêu chí PASS

- [x] Có đúng năm CSV chuẩn và schema tương thích.
- [x] README và tài liệu hiện hành ghi `brvehins1` là canonical source.
- [x] Validator xác minh `brvehins1` bằng static check header-only.
- [x] Legacy SUSEP được nêu rõ là non-canonical.
- [x] Không có file raw bị thay đổi.
- [x] `python scripts/validate_repo.py` đạt 39 PASS, 0 FAIL, 8 SKIPPED runtime scope.
- [x] `git diff --check` pass.

## Bước tiếp theo chính xác

`P1-DATA-01` — thực hiện EDA và source profiling trên năm partition, tạo bằng chứng máy đọc được trong `reports/data/`, sau đó đánh giá grain một cách trung thực.
